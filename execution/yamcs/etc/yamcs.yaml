services:
  - class: org.yamcs.http.HttpServer
    args:
      port: 8090
      zeroCopyEnabled: false

instances:
  - opensvf

dataDir: /tmp/yamcs-data-poc
# Dev-only placeholder, not for production.
secretKey: "orbitfabric-poc-dev"
